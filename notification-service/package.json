{
  "name": "notification-service",
  "version": "1.0.0",
  "description": "Consumes saga outcome events and exposes them as a notification feed (BFF).",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.check.json"
  },
  "dependencies": {
    "fastify": "^5.11.3",
    "nats": "^2.29.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
